<Page x:Class="Hotel_business.Pages.PaymentHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hotel_business.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaymentHistoryPage" Background="{StaticResource BackgroundColor}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="История платежей" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="5"/>

        <ListView x:Name="lvPayments" Grid.Row="1" Margin="5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Дата" Width="120" DisplayMemberBinding="{Binding PaymentDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
                    <GridViewColumn Header="Сумма" Width="100" DisplayMemberBinding="{Binding Amount, StringFormat={}{0:C}}"/>
                    <GridViewColumn Header="Способ" Width="100" DisplayMemberBinding="{Binding PaymentMethod}"/>
                    <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}"/>
                    
                </GridView>
            </ListView.View>
        </ListView>

        <Button Content="Назад" Grid.Row="2" Width="100" HorizontalAlignment="Center" Click="BtnBack_Click" Background="{StaticResource PrimaryColor}"/>
    </Grid>
</Page>

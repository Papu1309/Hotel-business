<Page x:Class="Hotel_business.Pages.ManageBookingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hotel_business.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ManageBookingsPage" Background="{StaticResource BackgroundColor}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление бронированиями" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" Margin="5"/>

        <ListView x:Name="lvBookings" Grid.Row="1" Margin="5" SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Гость" Width="120" DisplayMemberBinding="{Binding Users.Login}"/>
                    <GridViewColumn Header="Номер" Width="80" DisplayMemberBinding="{Binding Rooms.RoomNumber}"/>
                    <GridViewColumn Header="Заезд" Width="100" DisplayMemberBinding="{Binding StartDate, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <GridViewColumn Header="Выезд" Width="100" DisplayMemberBinding="{Binding EndDate, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Подтвердить" Click="BtnConfirm_Click"/>
            <Button Content="Отменить" Click="BtnCancel_Click"/>
            <Button Content="Назад" Click="BtnBack_Click" Background="{StaticResource PrimaryColor}"/>
        </StackPanel>
    </Grid>
</Page>
